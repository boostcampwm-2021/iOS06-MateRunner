excluded: # 린트 과정에서 무시할 파일 경로. `included`보다 우선순위 높음
  - Carthage
  - Pods
  - MateRunner/Application/AppDelegate.swift
  - MateRunner/Application/SceneDelegate.swift
  - MateRunner/Data/Network/DataMapping/MessagingRequestDTO.swift

disabled_rules:
  - trailing_whitespace
